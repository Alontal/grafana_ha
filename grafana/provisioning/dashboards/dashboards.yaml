apiVersion: 1

providers:
  - name: "Alerts"
    folder: "Alerts"
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/alerts

  - name: "Elasticsearch"
    folder: "Elasticsearch"
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/elasticsearch

  - name: "AWS"
    folder: "Aws"
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/aws

  - name: "MySQL"
    folder: "MySQL"
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/mysql
